<% provide(:title, 'Players') %>
<h1>Players</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @players %>
</ul>

<%= will_paginate %>

<% if current_user.admin? %>

	<%= link_to 'New Player', new_player_path %>
	
<% end %>
