<%= form_for(@group_stat) do |f| %>
  <% if @group_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_stat.errors.count, "error") %> prohibited this group_stat from being saved:</h2>

      <ul>
      <% @group_stat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  	 <%= f.collection_select(:group_id, Group.all, :id, :name, {}, :style => "width:300px; height:50px") %>
  </div>

  <div class="field">
  	 <%= f.collection_select(:team_id, Team.all, :id, :name, {}, :style => "width:300px; height:50px") %>
  </div>
  
  <div class="field">
    <%= f.label :matches_played %><br />
    <%= f.number_field :matches_played, defalt: 0 %>
  </div>
  <div class="field">
    <%= f.label :wins %><br />
    <%= f.number_field :wins %>
  </div>
  <div class="field">
    <%= f.label :draws %><br />
    <%= f.number_field :draws %>
  </div>
  <div class="field">
    <%= f.label :loses %><br />
    <%= f.number_field :loses %>
  </div>
  <div class="field">
    <%= f.label :goals_favor %><br />
    <%= f.number_field :goals_favor %>
  </div>
  <div class="field">
    <%= f.label :goals_against %><br />
    <%= f.number_field :goals_against %>
  </div>
  <div class="field">
    <%= f.label :points %><br />
    <%= f.number_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
