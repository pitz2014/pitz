<li>
  <%= link_to image_tag(country.flag_url , alt: country.name, size: 50), country %>
  <%= link_to image_tag(country.logo_url , alt: country.name, size: 50), country %>
  <ul class="span6">
  	<li><%= link_to country.name, country %></li>
  </ul>
  <% if current_user.admin? %>
    | <%= link_to "delete", country, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>      