<% provide(:title, 'Countries') %>
<h1>Countries</h1>

<%= will_paginate %>

<%= render @countries %>

<%= will_paginate %>

<% if current_user.admin? %>

	<%= link_to 'New Country', new_country_path %>
	
<% end %>
